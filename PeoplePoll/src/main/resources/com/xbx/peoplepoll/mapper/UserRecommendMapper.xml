<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xbx.peoplepoll.mapper.UserRecommendMapper">


    <select id="getById" resultType="com.xbx.peoplepoll.pojo.UserRecommendation">
        SELECT
        id,
        user_id,
        weibo_id,
        bid,
        screen_name,
        text,
        topic_label,
        location,
        score,
        recall_reason,
        create_time
        FROM v_recommendations
        WHERE user_id = #{userId}
        ORDER BY score DESC
    </select>
</mapper>